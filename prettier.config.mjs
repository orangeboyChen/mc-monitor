/** @type {import('prettier').Config} */
const config = {
    semi: false,
    singleQuote: true,
    jsxSingleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
    printWidth: 100,
}

export default config
